apiVersion: v1
kind: Service
metadata:
  name: {{ include "gp2f.fullname" . }}-server
  labels:
    {{- include "gp2f.labels" . | nindent 4 }}
    app.kubernetes.io/component: server
spec:
  type: {{ .Values.server.service.type }}
  ports:
    - port: {{ .Values.server.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gp2f.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: server
